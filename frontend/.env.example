# Vacío usa el proxy de Vite. Para apuntar el navegador a otra API, usa una URL HTTP(S).
VITE_API_BASE_URL=

# Solo lo consume el servidor de desarrollo de Vite.
TESSERAFLOW_BACKEND_URL=http://127.0.0.1:8000
